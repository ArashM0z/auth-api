# Dev override: hot reload inside the container — no local Node required.
# Usage: docker compose -f compose.yaml -f compose.dev.yaml up --build
services:
  api:
    build:
      target: dev
    environment:
      LOG_LEVEL: debug
    volumes:
      # Only the source tree is bind-mounted; node_modules stays in-image.
      - ./src:/app/src
